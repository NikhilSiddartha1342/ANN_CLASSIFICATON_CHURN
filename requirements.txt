# Streamlit Cloud compatible stack (works on Python 3.13 images)
streamlit==1.41.1
tensorflow==2.20.0
scikit-learn==1.6.1
pandas==2.2.3

# Keep numpy/protobuf unpinned so pip can resolve versions compatible
# with both Streamlit and TensorFlow for the current Python runtime.
